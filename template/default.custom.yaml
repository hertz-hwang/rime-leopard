# Rime default custom settings
# encoding: utf-8

patch:
  schema_list:
    - schema: leopard         # 好碼四定·常规
    - schema: leopard_smart   # 好碼四定·智能
    - schema: leopard_typeR   # 好碼四定·竞速
    - schema: hao             # 好碼三定
    - schema: qr-code         # 二维码
  switcher/save_options:
    - embeded_cands
    - full.word
    - full.char
    - full.off
    - charset.all
    - charset.freqly
  ascii_composer/switch_key:
    Caps_Lock: clear
    Control_L: noop
    Control_R: noop
    Eisu_toggle: clear
    Shift_L: commit_code
    Shift_R: commit_text